import React from 'react';
import { View, Text, StyleSheet} from 'react-native';

const ImageDetail = (props)=>{
    console.log(props);
 return <Text>Show Image Detail</Text>   
};

const styles = StyleSheet.create({});

export default ImageDetail;